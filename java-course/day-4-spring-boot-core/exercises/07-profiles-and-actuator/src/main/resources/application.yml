spring:
  application:
    name: payment-service-profiles

payment:
  gateway-url: "https://api.example-gateway.com/v1"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true

info:
  app:
    name: Payment Service
    version: 1.0.0
